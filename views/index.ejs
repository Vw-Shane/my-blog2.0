<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Blog</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
    <style>
    /*.post-container {
        cursor: pointer;
        max-width: 15vw;
        width: 100%;
        aspect-ratio: 1/1;
        margin-bottom: 20px;
        padding: 0.5vw;
        background: #f8f9fa;
        color: #333;
        margin-bottom: 0.5vw;
        border-radius: 10px;
    }*/

    .post-container img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 10px;
    }

    .intro {
        max-width: 100%;
    }

    .col-md-4 {

        padding-bottom: 30px;
    }

    .card-body {
        display: flex;
        flex-direction: column;
        justify-content: space-between;


        margin-bottom: 20px;
        padding: 0.5vw;
        /*        background: #f8f9fa;*/
        color: #f8f9fa;
        ;
        margin-bottom: 0.5vw;
        border-radius: 10px;
        /* Ensure the card body takes the full height of the card */
    }

    .category-card {
        position: relative;
        background-size: cover;
        background-position: center;
        color: white;
        overflow: hidden;
        border: none;
        border-radius: 0.25rem;
    }

    .card-body {
        position: relative;
        z-index: 1;
    }

    .category-card .card-body h2,
    .category-card .card-body h5,
    .category-card .card-body p {
        color: #f8f9fa;
        ;
        padding: 10px;
        border-radius: 0.25rem;
        background: rgba(0, 0, 0, 0.5);
        /* Dark overlay for readability */
    }

    .category-card.no-image .card-body h2,
    .category-card.no-image .card-body h5,
    .category-card.no-image .card-body p {
        background: transparent;
        /* No overlay if there's no image */
        color: black;
    }
    </style>
</head>
<!-- <body class="bg-dark text-white">
    <%- include('nav') %>
    <div class="container mt-2 intro">
        <h4 class="text-center">Welcome to my blog.<br>Below you'll see recent and pinned posts, as well a link to my merch store.<br>Up above you can go to specific blog categories and see all the posts associated to them.<br>Feel free to subscribe to them all at this link or subscribe to individual ones from within the above categories.<br> Enjoy!</h4>
    </div>
    <div class="container mt-5">
        <div class="posts mt-4">
 <div class="posts mt-4">
            <div class="row">
                <% categories.forEach(category => { %>
                <% const recentEntry = entries[category.id]; %>
                <% if (recentEntry) { %>
                <div class="col-md-4">
                    <h2 class="category-header"><%= category.categoryname %></h2>
                    <div class="post-container" onclick="window.location.href='/post/<%= recentEntry.category_id %>/<%= recentEntry.post_id || recentEntry.project_id %>'">
                        <h3><%= recentEntry.title %></h3>
                        <p><%= new Date(recentEntry.createdate).toLocaleDateString() %></p>

                        <% if (recentEntry.photo_link) { %>
                        <img src="<%= recentEntry.photo_link %>" class="img-fluid" alt="Post Image">
                        <% } %>
                    </div>
                </div>
                <% } %>
                <% }); %>
            </div>
        </div>
        </div>
    </div>
</body> -->

<body class="bg-dark text-white">
    <%- include('nav') %>
    <div class="container mt-2 intro">
        <h4 class="text-center">
            Welcome to my blog!<br>
            Here, you’ll find recent and pinned posts, plus a link to my store.<br>
            Dive into specific topics and categories along with their related posts using the menu above.<br>
            Feel free to <a href="[subscription-link]">subscribe</a> to all categories or choose individual topics.<br>
            Enjoy!
        </h4>
    </div>
    <div class="container mt-5">
        <div class="posts mt-4">
            <div class="row">
                <% categories.forEach(category => { %>
                <% const recentEntry = entries[category.id]; %>
                <% if (recentEntry) { %>
                <div class="col-md-4">
                    <div class="card category-card <%= recentEntry.photo_link ? '' : 'no-image' %>" style="<%= recentEntry.photo_link ? `background-image: url('${recentEntry.photo_link}');` : '' %>" onclick="window.location.href='/post/<%= recentEntry.category_id %>/<%= recentEntry.post_id || recentEntry.project_id %>'">
                        <div class="card-body">
                            <h2 class="category-header">
                                <%= category.categoryname %>
                            </h2>
                            <div class="post-container">
                                <h5>
                                    <%= recentEntry.title %>
                                </h5>
                                <p>
                                    <%= new Date(recentEntry.createdate).toLocaleDateString() %>
                                </p>
                            </div>
                            <!--   <% if (recentEntry.photo_link) { %>
                            <img src="<%= recentEntry.photo_link %>" class="img-fluid" alt="Post Image">
                            <% } %> -->
                        </div>
                    </div>
                </div>
                <% } %>
                <% }); %>
            </div>
        </div>
    </div>
</body>

</html>